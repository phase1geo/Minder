i18n.gettext(meson.project_name(),
    args: [
        '--directory='+meson.source_root(), 
        '--from-code=UTF-8',
        join_paths(meson.source_root(), 'data', meson.project_name()+'.shortcuts.ui'),
    ]
)

subdir('extra')
